#!/usr/bin/env bash

set -euo pipefail

SOURCE="$HOME/sync-poufne"
DEST="$HOME/backup/sync-poufne"

TIMESTAMP="$(date '+%Y-%m-%d_%H-%M-%S')"
BACKUP="$DEST/$TIMESTAMP"

mkdir -p "$BACKUP"

rsync -a "$SOURCE/" "$BACKUP/"
